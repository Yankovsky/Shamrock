@{               
    ViewBag.Title = Resource.HomeTitle;
}

@section LeftPanel
{
    @Html.Partial("_Picture")
    @Html.Action("GetImageLinksBlock", "ImageLink", new { imageLinksBlockName = "leftBannersBlock" })
}

@Html.Action("ClosestEvent", "Events")

@Html.Action("GetImageLinksBlock", "ImageLink", new { imageLinksBlockName = "rightBannersBlock" })

@section PreFooter
{
    @if (User.Identity.IsAuthenticated)
    { 
        @Html.ActionLink(Resource.ImageLinksTitle, "Index", "ImageLink", null, new { @class = "mediumButton" })
    }
}